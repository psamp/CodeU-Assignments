package com.psamp.bucketlist;

import java.util.Set;

public class BucketList {
	
	Set<String> bucketList = new Set<String>();

	public BucketList(String...strings) {
		
		for (int i = 0; i < strings.length; i++) {
			bucketList.add(strings[i]);
		}
		
	}

}
