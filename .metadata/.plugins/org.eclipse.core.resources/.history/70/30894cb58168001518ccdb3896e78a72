package com.psamp.ktom;

public class Methods {
	
	//Take in a mile, and convert it into kilometers ([miles] * 1.6.)
	public static String milesToKilos(double miles) {
		double result;
		
		result = miles * 1.6;
		
		return String.format(miles + " miles" + " is %.5f", result);
	}
}
